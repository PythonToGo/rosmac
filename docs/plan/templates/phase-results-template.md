# Phase N 결과 리포트 — <날짜 YYYY-MM-DD>

> 사본을 `docs/plan/phaseN-results.md`로 만들어 채운다. 태스크 하나 끝날 때마다 갱신
> (페이즈 끝나고 몰아서 쓰지 말 것 — 버전/에러 전문은 그 순간에만 정확하다).

## 0. 실행 환경
- macOS 버전 / 칩 / RAM:
- 실행 에이전트(모델명) 및 날짜:
- 도구 버전: lima= / micromamba= / (해당 시) zenoh-bridge= / python=

## 태스크별 기록 (태스크당 한 블록)

### PN.M <태스크 이름> — PASS | PARTIAL | FAIL
- 완료 기준(AC) 체크리스트: 문서의 AC를 복사해 [x]/[ ]로 표기
- 확정/핀한 것: (채널명, 버전, URL, 경로 등 — 이후 태스크가 참조할 값)
- 계획 문서와 다르게 수행한 것 + 사유: (없으면 "없음")
- 측정값: (해당 시 — 3회 측정 중앙값, 단위 명시)
- 발견한 함정: (known-issues.md에 추가한 항목 번호)
- 에러 전문(실패/우회 시): ```로 원문 붙여넣기
- 증거: 명령 출력 스니펫, 스크린샷 경로(docs/plan/evidence/)

## 게이트 결정 (게이트가 있는 페이즈만)
- 판정: GO | GO(수정: ...) | NO-GO(대안: ...)
- 판정 근거:
- PLAN.md 반영 사항: (갱신한 D#/R# 목록, 없으면 "없음")

## 다음 페이즈 인계 메모
- 다음 실행자가 알아야 할 것 3줄 이내:
