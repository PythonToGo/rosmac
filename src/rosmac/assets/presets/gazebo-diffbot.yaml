name: gazebo-diffbot
description: "Gazebo Fortress headless diff drive (drive /cmd_vel from the Mac, /odom via Foxglove)"
vm_apt:
  - ros-humble-ros-gz
  - ros-humble-ros-gz-sim-demos
vm_env: {}
launch:
  cmd: "ros2 launch ~/rosmac-presets/gazebo-diffbot/diffbot.launch.py"
foxglove_layout: diffbot.json
health_topics:
  - { name: /odom, timeout: 45 }
